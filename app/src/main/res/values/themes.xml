<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Getkracking" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="iconTint">@color/white</item>
    </style>

    <style name="Theme.Getkracking.NoActionBar"/>

    <style name="Theme.Getkracking.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="customTextNoSpecificCaseLayout" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:textAllCaps">false</item>
        <item name="font">@font/notosans_regular</item>
        <item name="android:textSize">18dp</item>
        <item name="android:textColor">@color/primary</item>
    </style>

    <style name="CustomTextUppercaseLayout" parent="TextAppearance.Design.Tab">
        <item name="font">@font/notosans_regular</item>
        <item name="android:textSize">18dp</item>
    </style>

    <style name="CustomRatingBarStyle" parent="Widget.AppCompat.RatingBar">
        <item name="android:progressDrawable">@drawable/customrating</item>
        <item name="android:minHeight">15dip</item>
        <item name="android:maxHeight">47dip</item>
    </style>

</resources>